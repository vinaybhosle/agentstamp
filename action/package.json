{
  "name": "@agentstamp/verify-agent",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action to verify AI agent trust score via AgentStamp",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.js -o dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "vitest": "^4.1.0"
  }
}
